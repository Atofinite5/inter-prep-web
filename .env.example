# Backend API base URL (no trailing slash handling needed — code appends paths)
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:5001/api
# Production example:
# NEXT_PUBLIC_API_URL=https://your-api-host.com/api
